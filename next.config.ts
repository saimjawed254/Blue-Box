import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["img.clerk.com", "images.clerk.dev", "source.unsplash.com", "unsplash.com", "loremflickr.com", "picsum.photos"],
  },
};

export default nextConfig;
